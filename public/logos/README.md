# Hospital Logos

Place default hospital logo files here when the app is deployed with a fixed
brand template.

The current prototype also lets users choose a logo directly from the browser,
so this folder can stay empty during early development.
