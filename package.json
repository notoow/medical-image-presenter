{
  "name": "medical-image-presenter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "check": "node --check src/app.js"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
